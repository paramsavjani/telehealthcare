import { FC } from "react";
import AddFreindsuButton from "@/components/AddFriendButton";

const page: FC = ({}) => {
    return (
        <main>
            <AddFreindsuButton />
        </main>
    );
};

export default page;
